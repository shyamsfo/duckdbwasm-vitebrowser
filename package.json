{
  "name": "@nudata/nu-duck",
  "version": "0.1.1",
  "type": "module",
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=20"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions",
    "last 2 Safari versions"
  ],
  "license": "MIT",
  "main": "./dist/duck_wasm.js",
  "exports": {
    ".": "./dist/duck_wasm.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:watch": "vite build --watch",
    "preview": "vite preview",
    "clean": "rimraf dist",
    "test": "playwright test",
    "prepublishOnly": "yarn clean && yarn build",
    "postpublish": "git push && git push --tags",
    "showtags": "yarn tag list @nudata/nu-duck",
    "tl": "yarn tag list",
    "tl_2": "npm dist-tag ls @nudata/nu-duck latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "rimraf": "^6.1.3",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "1.33.1-dev42.0"
  }
}
